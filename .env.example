# NuExtract API Configuration
# Get your API credentials at https://nuextract.ai/

# API endpoint URL (your NuExtract project endpoint)
NUEXTRACT_API_URL=https://api.nuextract.ai/v1

# Your NuExtract API key
NUEXTRACT_API_KEY=sk-your-api-key-here

# Request timeout in milliseconds (default: 30000)
NUEXTRACT_TIMEOUT_MS=30000

# PassportEye Microservice Configuration
# Deploy the passporteye-service to Railway/Render/Fly.io and set the URL here

# API endpoint URL for PassportEye service (e.g., https://passporteye.railway.app/extract)
PASSPORTEYE_API_URL=https://your-passporteye-service.railway.app/extract

# Request timeout in milliseconds (default: 30000)
PASSPORTEYE_TIMEOUT_MS=30000

# Enable/disable PassportEye extraction (set to false to skip, default: true)
PASSPORTEYE_ENABLED=true

# G-28 Claude Extraction Service
# Deploy g28-extraction-service (uses Claude Vision API for form extraction)

# API endpoint URL for G-28 Claude extraction service
G28_CLAUDE_API_URL=http://localhost:8001/extract

# Request timeout in milliseconds (default: 60000 - longer for Vision API)
G28_CLAUDE_TIMEOUT_MS=60000

# Enable/disable G-28 Claude extraction (set to false to use NuExtract fallback only)
G28_CLAUDE_ENABLED=true

# Note: The g28-extraction-service requires ANTHROPIC_API_KEY to be set
# This should be configured in the Docker environment or passed via docker-compose

# Form Automation Service
# Deploy form-automation-service for Playwright-based form filling

# API endpoint URL for form automation service
FORM_AUTOMATION_API_URL=http://localhost:8002/fill

# Request timeout in milliseconds (default: 120000 - 2 minutes for browser automation)
FORM_AUTOMATION_TIMEOUT_MS=120000

# Enable/disable form automation (set to false to disable)
FORM_AUTOMATION_ENABLED=true
